export const VERBOSITY = {
  SILENT: 'silent',
  NORMAL: 'normal',
  VERBOSE: 'verbose'
};

export const enum AUTH_ACTION {
  LOGIN = 'LOGIN',
  LOGOUT = 'LOGOUT',
  FETCHING = 'FETCHING',
  ERROR = 'ERROR'
}
